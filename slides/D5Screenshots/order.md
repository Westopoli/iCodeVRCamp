read order of screenshots as they're taken

Started with slot and adding a key
Then showed how to make a teleportation happen when the door is opened (mention that teleport can be attached to any object that has the "On Place" property in it's properties (bottom right))

Then moved to placing a briefcase, making the lid an animation instead of a button.

Placed lock in front of briefcase, (make sure to mention to change lock combo, each individual box is a single digit in the lock. Can't be 1234, neesd to be 1 seperated 2 seperated in another box ect.) visual will make sense with that explanation. 

Painted the code on the wall next with pain tool, tell them they can hide this somewhere or do whatever they want. Now they have the ability to do really any combination of things. 

Regular objects can be used in slots instead of just keys, position object can be placed in slot is editable. 

The finish logic works like a lock, just set the "when opened" or "on place" property to activate the "final flag" logic object. 
Water exists and can be placed in the game. Locks can manually be created if needed. Spawn point can be placed. That's about it.  